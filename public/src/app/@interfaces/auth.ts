export interface RegisterData {
    login: string;
    password: string;
    confirmPassword: string;
    email: string;
    name: string;
}

export interface LoginData {
    login: string;
    password: string;
}

